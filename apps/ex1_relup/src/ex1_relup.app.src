{application, ex1_relup,
 [{description, "ex1_relup: example of a separate configuration delivery pipeline design"},
  {vsn, "0.4.0"},
  {registered, [config, watcher, subscription_manager, config_sup, watcher_sup]},
  {mod, {ex1_relup_app, []}},
  {applications,
   [kernel,
    stdlib
   ]},
  {licenses, ["BSD 3-clause clear"]}
 ]}.
